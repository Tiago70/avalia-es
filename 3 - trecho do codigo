#include <stdio.h>

int main()
{
    int indice = 12, k = 1, soma = 0;
    while (k < indice){
        k = k + 1;
        soma = soma + k;
    }
    printf("%d", soma);
    return 0;
}

//resultado: 77
